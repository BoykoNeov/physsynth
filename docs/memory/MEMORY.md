# Memory index

- [Milestone 1 state](milestone-1-state.md) — ideal string + harness built & passing; decisions taken vs. still-open (§11); language resolved (stay Python, port deferred) + portability contract; remote `origin` exists (public), CI runs on push
- [Stiff string state](stiff-string-state.md) — Phase-2 model #2 (implicit θ-scheme) built & passing (91 tests); biharmonic=D2², energy via same matrix, θ=0.28, λ>1 allowed; damping caveat 2σ(1−θQk²) cured by model #3; portability-test fix needs human review
- [Damped string state](damped-string-state.md) — Phase-2 model #3 (freq-dependent loss, +2σ₁u_txx) built & passing (118 tests total); A gains −σ₁k·D2 (still SPD), energy form unchanged → passivity automatic, σ₁=0 bit-for-bit StiffString; per-mode g_m oracle is the tight money test; rate turns over past m≈32, θ-artifact symptom-cured not fixed; string family #1–3 done, next Phase 3 (2D)
- [Membrane state](membrane-state.md) — Phase-3 model #4 (2D circular drumhead, staircased Dirichlet) built & passing (160 tests total); energy⊥geometry (masked Laplacian symmetric → staircased circle conserves like rectangle), 2D CFL λ≤1/√2, no dispersionless λ (anisotropic), Bessel = convergence-rate tier (~O(h), −9c at N=128); decisions all asked not guessed; next = interactive web viewer, then model #5 plate
- [Web viewer state](web-viewer-state.md) — Phase-3.5 interactive viewer (architecture B: local Python backend + browser, offline recompute) — string Phase A + membrane Phase B + **Phase C (plates #5/#5b + von Kármán #6 gong/cymbal)** ALL wired, tested (69 web / 392 total), browser-verified 7/7; core untouched, wrappers in web/; decisions from advisor catches — payload (48k audio, slow-mo, σ-gated energy) + membrane (mode-spectrum markers, decimation, N-cliff) + Phase-C (domain-select=boundary, plate μ-Courant guard points to RAISE μ, eigen helper duck-types on .W supported eigsh(-L)/free drop-3, **VK energy Picard-convergence-gated** both branches, **VK spectrum=hardening-shift not cents**, supported VK **mode11 IC** or zero-cross overcounts, free VK crash=no f0, κ/ρ re-range on model-switch, data-scale E/GPa e/mm)
- [Plate state](plate-state.md) — Phase-3 model #5 (Kirchhoff plate, SS rectangle, implicit θ-scheme) built & passing (231 tests); plate = membrane Laplacian + stiff-string θ-scheme + biharmonic B=L@L (bakes in SS BCs, sin·sin exact eigenvector eigenvalue Λ²); bending-ONLY (no wave term), no κ=0 anchor (money test = B eig==Λ², 2.4e-13), damping caveat BROADER (Q=κ²Λ² 4th-power whole spectrum), solver=splu not cholesky_banded, ν drops out for SS, TIGHT 1-cent bar held; Ly-snapping gotcha bit diagnose script; next = free-edge Chladni / web-viewer plate panel, then model #6 nonlinear plate
- [Free plate state](free-plate-state.md) — Phase-3 model #5b (2D free-edge FFFF Kirchhoff plate + curved Chladni) built & passing (294 tests); first 2D free-boundary resonator; energy-first K (bending-diagonal == free-beam op per axis via kron, cell-centered twist `kron(d1y,d1x)`, Wa=kron(m_y,m_x)→edge½/corner¼), **only-new-code-was-ν-coupling** (advisor collapse); nullspace exactly {1,x,y}, K(xy)∝(1−ν), exactly-3-zero-modes catch, kron==dense-assembly; Leissa FFFF-square matched 0.01% at N=80 (cite Narita 2022, improves on Leissa monograph); saddle fundamental not bulge; portability allowlist test fixed (delta not absolute — pywin32 startup baggage) flagged for human; next = model #6 nonlinear von Kármán plate
- [Beam state](beam-state.md) — Phase-3 model #5b-pre (1D free-free Euler-Bernoulli beam) built & passing (263 tests); **first FREE-boundary resonator**, the free-edge-plate de-risk; energy-first K=h·D2ᵀD2 (Gram product) → symmetry+natural BCs+{1,x} nullspace by construction; **free-end closure comes from half-cell end masses W=diag(h/2,…,h/2), NOT hand-coded stiffness rows** (W⁻¹K==Bilbao bar; expected to recur in 2D edge-½/corner-¼ W); θ-scheme A SPD via SPD W (K only PSD), splu not cholesky, no CFL; closed-form oracle cosβL·coshβL=1 (brentq on cos−sech), neg shift for eigsh; broad damping caveat 2σ low-only; order→2.00, drift 2–5e-12; next = Part 1 2D free plate (task #3, flip test_plate_stability free-raises)
- [Von Kármán plate state](von-karman-plate-state.md) — Phase-4 model #6 (nonlinear plate): **Parts 1–5 BUILT & GREEN** (350 tests; P5 diagnostics 2026-07-02). P1 = bracket `VonKarmanBracket` + triple-self-adjointness (empirical stencil; cell-centered twist; rim-vanishing contract; O(h²)). **P2 = `AiryStressSolver` F-solve, CLAMPED `F=0,F,n=0`** (DT Eq.11; NOT `L²`/Navier); energy-first `B_F=Lc_rᵀ Wa Lc_r`; **trapezoidal `Wa` load-bearing**; Wa-weighted Galerkin RHS. **P3 = `VKPlate` coupled resonator (`core/plate.py`), materials surface `(E,e,ν,ρ)`** (human decision #3; derive ρ_s,D,κ,Y=Ee); **conservative IMPLICIT Picard scheme** `l(μ_{t·}w,μ_{t·}F)` (F^m from w^m) telescopes exactly via P1 self-adjointness — **NOT** the plan's explicit `l(w^n,F^n)` (quartic potential → explicit drifts O(k²)); ½-averaged energy (odd/even fix); live↔full-grid seam; `nonlinear=False` **bit-identical** to model #5. Gates: drift **2.6e-13** @ membrane 57 %, **drift∝couple_tol** self-cert, passivity exact, w→0→#5, pitch-glide +74 %, Richardson **4.40**. **P5 = `scripts/diagnose_vk_plate.py` + 3 `viz` helpers** (energy breakdown flat-total-over-anti-correlated-exchange drift 8.4e-13, `w/e` sweep zero-crossing f0 +75 % by w=5e, σ=3 spectrogram glide ~370→214 Hz, `w`+`F` stress snapshot; zero-crossing NOT `measure_partials_near`—advisor). **P6 = free-edge cymbal `VKPlate(boundary="free")` DONE 2026-07-02, MODEL #6 COMPLETE:** crux (bracket self-adjoint only rim-vanishing, but free `w≠0` rim) dissolves because Airy `F` is still clamped-zero → need only swap identity `⟨l(x,F),g⟩=⟨l(x,g),F⟩` (F rim-vanishing) → **bracket + clamped Airy REUSED VERBATIM**; **mixed weighting** (mass `W` but coupling pairs uniform-h², `H_mem` secretly uniform-h²): `step()` RHS gains `h²` (`k²h²l/ρ_s`), `set_state` `w^{-1}` gets `h²/ρ_s` AND per-node `/W`; `boundary="free"` branch (not new class), `nonlinear=False`→#5b bit-identical; gates drift **1.67e-13** @ membrane 57 %, small-amp→#5b 2.8e-13, glide +33 % (eigenmode-IC+FFT ≤3e), Richardson **5.66**; `tests/test_vk_free.py` (12), `scripts/diagnose_vk_free_plate.py` (curved-Chladni + tonal-vs-crash cascade). Full suite **362**
- [Body/bridge state](body-bridge-state.md) — Body/radiation node built & green (**439 tests**). Steps 1-3 (modal body + string-terminus bridge + radiation): **explicit linear spring is EXACT** (one linear leapfrog → E_string+E_body+E_conn to machine precision, advisor reconciled vs VK); **cheap 2-DOF guard is a footgun** → exact coupled-eigenvalue guard k²λ_max(A)<4; β_s=2k²/ρh end h/2; pressure()=Σaᵢ·2nd-diff-accel (verified under coupling); per-end ("fixed","free") string; K=0 bit-identical. **Step 4 DONE (2026-07-02): `StringPlateBridge` = grid plate #5 as a distributed body** via single **driving-point** node; advisor said build grid-plate not modal-reduce (dead end). Crux = explicit string+spring → **implicit** plate: string reaction post-step, plate `+F` into θ-scheme **RHS before solve** (`Plate.step(f_ext)`) as k²F/(ρ_s h²); θ-avg touches only elastic → time-n source gives kF·δw_dp ∀θ → telescopes. **Guard = EXACT Sherman-Morrison** (plate block PD ∀θ≥¼ = *why* implicit is unconditional → only rank-1 spring destabilises): `(k²/4)K[(G0_str⁻¹)_end+(G0_plate⁻¹)_dp]<1`, **margin LINEAR in K**; `Plate` gained `_accel`+`pressure()=h²Σaccel`; drift 1.7e-13, plate 86% energy, K=0 bit-identical, 231 plate tests untouched; `tests/test_plate_connection.py` (23), `scripts/diagnose_string_plate.py` (terminus 54.8→98 Hz); **Step 5 DONE → see [Free-plate bridge state]**
- [Free-plate bridge state](free-plate-bridge-state.md) — Step 5 of body/radiation node (2026-07-02): free-edge plate #5b (suspended cymbal/gong) as a distributed body. **Generalized `StringPlateBridge` to `boundary="free"`, NOT a new class** (mirrors `VKPlate(boundary="free")`); `plate.py` **zero edits** (step/energy/pressure already branch on boundary) → **only the guard's plate block changed**. General guard form `G0=M+(θ−¼)k²S` (string block = θ=0 case); free = **`ρ_s[W+(θ−¼)k²κ²K]`, NO extra h²** (W already carries area weight — the paste gotcha), PD because diagonal W anchors the {1,x,y} K-nullspace. **No rigid drift** (advisor): point-force orthogonal to `w_dp=0` zero-modes → never excited from rest → same ~1e-14 drift as SS bridge. Force injection unchanged (`k²f/ρ`, W-divide via A-solve). Gates: margin identical to SS (0.2148), free plate 89% energy, divergence-verified ceiling K≈13.97k (bypass diverges by step ~104). `tests/test_free_plate_connection.py` (23), `scripts/diagnose_string_free_plate.py`, `make_free_plate_bridge` (nu plumbed). **Closes body-coupling family (Steps 1–5).**
- [Commit & push at batch end](commit-push-at-batch-end.md) — standing rule: end of batch / "session end" → update memory+docs, commit, push to main; **"commit" always pushes too (paired, never split)** (remote `origin` configured, CI runs)
- [Respect ruff line length](respect-ruff-line-length.md) — feedback: keep every Python line ≤100 chars in the FIRST draft (ruff E501, line-length=100; CI runs `ruff check .` as a fail-fast gate); never write over-length lines then reflow — that's wasted-token churn
