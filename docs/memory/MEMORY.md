# Memory index

One line per memory — a hook to decide relevance, not the content. Open the file for the detail.

## Working rules & preferences

- [Commit & push at batch end](commit-push-at-batch-end.md) — standing rule: batch/session end → update memory+docs, commit, push; "commit" always pushes too (never split)
- [Respect ruff line length](respect-ruff-line-length.md) — ≤100 chars in the FIRST draft; CI runs `ruff check .` as a fail-fast gate; never write long then reflow
- [Destructive undo discipline](destructive-undo-discipline.md) — never undo a temp edit with `git checkout -- <file>`; it restored HEAD and DISCARDED a whole batch's uncommitted core work — revert the edit itself, and commit a checkpoint before deliberately breaking code
- [Identify processes before killing](identify-processes-before-killing.md) — read the command line first; this machine runs the user's own Python, I killed two of theirs assuming they were my pytest orphans
- [Port reclaim modus operandi](port-reclaim-modus-operandi.md) — port busy? identify the PID's command line; reclaim only if it's a stale run of THIS program, else try the next port
- [Unphysical params are a feature](unphysical-params-are-a-feature.md) — prefer effective-coefficient APIs that permit inconsistent combos over materials surfaces that forbid them; offer realism via a helper, never impose it

## Project state

- [CI runner variance](ci-runner-variance.md) — GitHub runner class varies **~1.6×**; an untouched test read 252/252/158/252s across four runs, so normalise against an unchanged reference before attributing any wall-clock delta — and **same-run ≠ comparable**: every job is its own machine, so only a *within-job* comparison is variance-immune (corrected 2026-08-17)
- [Test suite performance](test-suite-performance.md) — **1808** tests / **5027.9 core-seconds** (2026-08-17), gate now **3 concurrent shards**, 15-21 min → ~5-8; the "two pinned modules are the critical path" story was **WRONG** — the tail was never priced and the suite runs at **99% core utilisation**, i.e. BULK-bound; the coarse module pin only became expensive *after* sharding (a file that WAS its shard's wall to within 1%) and is now **one group per fixture**; shard membership is **computed from the glob**, never listed, with a guard inside (coverage) and one outside (collect-and-subtract); **each shard is its own machine, so two shards are as incomparable as two runs**; fast lane DELETED (3.11 → nightly); **never pass `-q`**
- [Milestone 1 state](milestone-1-state.md) — ideal string + validation harness; language resolved (Python, port deferred); remote `origin` + CI on push; its "still the human's to set" list is retired by ↓
- [HANDOFF §11 decisions closed](handoff-decisions-closed.md) — **all five closed 2026-08-10, doc-only, zero code**; THREE were already answered by practice (check history before asking the human); polyphony = **per-instance** for field models vs **per-voice** for strings, budget half deferred to Phase 5; tolerances **STAND** — the 1e-10 bar is the native port's acceptance contract, so the 50× gap to observed is headroom, **do not tighten tier 1**
- [Stiff string state](stiff-string-state.md) — model #2, implicit θ-scheme, biharmonic=D2²; damping caveat cured by #3; also the **core-dep allowlist policy**: hardcoded set (human's call, for visibility) + measured as the sys.modules DELTA, so a new platform name is a deliberate edit
- [Damped string state](damped-string-state.md) — model #3, freq-dependent loss (+2σ₁u_txx); per-mode g_m oracle is the money test; σ₁=0 is bit-identical to #2; θ-artifact symptom-cured not fixed
- [Membrane state](membrane-state.md) — model #4, 2D circular drumhead, staircased Dirichlet; **energy ⊥ geometry**; 2D CFL λ≤1/√2, no dispersionless λ; Bessel = convergence-rate tier
- [Plate state](plate-state.md) — model #5, Kirchhoff SS rectangle; = membrane Laplacian + stiff-string θ-scheme + biharmonic B=L@L; no κ=0 anchor; damping caveat broader (4th-power)
- [Orthotropic plate state](orthotropic-plate-state.md) — model #5o, the plate gets a **grain** (wood not metal), 2026-08-17; the trap is where the **factor of 2** lives; both planned headlines DIED — it detunes **selectively** rather than reordering, and the grain is in the **partial series not the level**; **three detectors, three blind spots** — incl. a SQUARE plate's **diagonal modes are invariant under the grain running 90° wrong**
- [Beam state](beam-state.md) — model #5b-pre, 1D free-free Euler-Bernoulli; the **first FREE-boundary** resonator (de-risks the free plate); free-end closure comes from half-cell end MASSES, not stiffness rows
- [Free plate state](free-plate-state.md) — model #5b, 2D free-edge FFFF + curved Chladni; nullspace exactly {1,x,y}; matched Narita/Leissa 0.01%; fundamental is the **saddle/twist** not a bulge; cited λ digits beat memory
- [Free-plate orthotropic state](free-plate-orthotropic-state.md) — model #5of, the **free** plate gets the grain, 2026-08-17; a free edge needs **FOUR** constants and sees the `H`-split the supported plate provably cannot (**bit-identical `0.0`** vs **6.5×**); here the grain **DOES reorder** modes (contra #5o); validation had to be **derived** (paywalled tables) as four probes, one per constant; scars: a **superseded-but-still-validated argument is a live constraint**, and "exact in exact arithmetic" ≠ exact in doubles at `h²`×`1/h⁴`
- [Von Kármán plate state](von-karman-plate-state.md) — model #6 nonlinear plate, Parts 1–6 COMPLETE (gong + free cymbal); conservative IMPLICIT Picard (explicit drifts); `nonlinear=False` bit-identical to #5/#5b
- [Tension string state](tension-string-state.md) — model #9, Kirchhoff–Carrier; the string family's nonlinearity; EXACT Duffing oracle; **single-mode motion is parametrically unstable above ΔT/T₀≈3** (real physics); energy=STRUCTURAL, purity=DYNAMICAL
- [Geometric string state](geometric-string-state.md) — model #10 (u,w,v), COMPLETE; exact geometry, discharges #9's two refusals (phantom partials + whirling); **`λ_long` is THE trap**; whirl = Mathieu tongue
- [Bow state](bow-state.md) — bowed string = first continuous nonlinear EXCITER; closes the exciter leg; energy = BALANCE not conservation; σ₁>0 REQUIRED for clean 1-slip Helmholtz; Schelleng window is real
- [Bore state](bore-state.md) — wind leg batches 1–2: linear acoustic bore + radiating bell (clarinet); staggered p-U leapfrog, Webster S(x); reflection oracle; reed = [[reed-state]]
- [Reed state](reed-state.md) — wind batch 3, dynamic single reed; the wind self-oscillating exciter, acoustic dual of the bow; balance is NOT sufficient (a dead reed passes on silence) → signature oracle
- [Mallet collision state](mallet-collision-state.md) — model #7, mallet→membrane; the FIRST contact model; CONSERVATION via energy-conserving discrete-gradient force + the **0/0 Taylor branch** (the #1 trap)
- [Barrier collision state](barrier-collision-state.md) — model #8, string↔distributed barrier (fret buzz); vector Newton + Armijo; static-equilibrium magnitude oracle; **scipy lu_solve not np.linalg.solve** (NumPy 2.4 BLAS cliff)
- [Jawari state](jawari-state.md) — sitar/tanpura buzzing bridge = a CONFIGURATION of model #8, zero core code; depth must ≈ near-termination downswing; sustained shimmer + travelling wrap
- [Juari state](juari-state.md) — tanpura cotton thread = SINGLE-NODE point contact (viewer batch 14), the jawari's position-SELECTIVE complement; headline = the TUNING CURVE not "more buzz"; buzz is SETTLED so the sweep is a canonical duration decoupled from audio; grid snap = the resolution
- [Sympathetic strings state](sympathetic-strings-state.md) — coupled-string batch 1: N strings sharing one bridge point; zero ModalBody edits; money oracle = the antisymmetric normal mode; LINEAR only
- [Body/bridge state](body-bridge-state.md) — body/radiation Steps 1–4: modal body + string terminus + grid plate #5 as a distributed body; explicit linear spring is EXACT; guard = exact Sherman-Morrison
- [Free-plate bridge state](free-plate-bridge-state.md) — Step 5: free plate #5b as a distributed body (suspended cymbal); generalized the bridge, `plate.py` zero edits; closes the body-coupling family
- [String↔VK plate bridge state](string-vk-bridge-state.md) — the gong on a string (§12H's last open model, 2026-08-17); the exact linear guard stays SUFFICIENT but the failure mode **migrates to Picard non-convergence**, which a quadratic form can't see; want the gong impression, use a **mallet**
- [String→gong→room chain](string-vk-room-chain-state.md) — the three-way chain, 2026-08-17, **zero core edits**; the plan's third fixed point **does not exist**; `w/e` is **not an amplitude** under a point force (the 781× was wrong); the replacement claim is **band overlap, not amplitude**; and a `drive_index` differing between two runs fools **all three detectors** — they are jointly insufficient against a COMPARISON
- [Air box state](air-box-state.md) — 3-D FDTD room (§12H), **batches 1–6 all shipped, model list COMPLETE** (b6 2026-08-17: gong + cymbal); the family's standing rule is that **no single one of the three detectors is sufficient** (four batches, four different blind spots); the cost runs the wrong way twice over — **coarsening the room breaks the plate's fixed point**; scar: `git checkout -- <file>` discarded a whole batch
- [Radiation state](radiation-state.md) — the air node in three tiers: read-out `AirRadiation` · constant-R load `RadiatedBody` · **batch 3 `RationalAirLoad` = the EXACT rational impedance** (R ∥ radiation mass, one aux state, the network IS the passivity proof); air now STORES as well as dissipates; the reactance flattens the pitch and my α oracle was 26% off without it — **surfaced in the viewer 2026-08-10 as `airload`, and batch 2's R vs batch 3's R are DIFFERENT quantities** (compact-source at one frequency vs saturated plane-wave; they meet only at ka=1, so batch 2's honesty note was RIGHT)
- [Airbox viewer state](airbox-viewer-state.md) — viewer batch 18 `airbox`: `dims:3` is a **SLICE SET** not a volume; the claim is the **lattice light cone** (an exact integer, λ-INDEPENDENT, beating both physical arrivals); cost is **h⁻⁴** because the 3-D CFL runs the WRONG way, so the ROOM sets fs and the string's λ is DERIVED; a TRANSPOSED slice decode renders as plausible BANDING and a NaN dies only at the JSON boundary
- [Web viewer state](web-viewer-state.md) — Phase-3.5 interactive viewer (local Python backend + browser); Phases A–C plus **Phase D batches b1–b17**, model list CLOSED (b17 `airload` 2026-08-10); Phase D = **consolidation** (surface built-but-unshown models over adding physics); **the backend does NOT hot-reload**; still open: Phase 5 real-time port · a §12 thread
